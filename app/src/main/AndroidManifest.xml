<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoAnNhom7">
        <activity android:name=".ui.admin.AdminMainActivity" />
        <activity android:name=".ui.user.UserMainActivity" />
        <activity android:name=".ui.admin.AdminCategoryFormActivity" />
        <activity android:name=".ui.admin.AdminCustomerFormActivity" />
        <activity android:name=".ui.admin.AdminProductFormActivity" />
        <activity android:name=".ui.user.UserOutInvoicesActivity" />
        <activity android:name=".ui.user.UserOutInvoiceEditActivity" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>